<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Warlordlands - <%= typeof title !== 'undefined' ? title : 'Epic Strategy Game' %></title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Custom Game Styles -->
    <link rel="stylesheet" href="/css/styles.css">
    
    <!-- Page-specific CSS -->
    <% if (typeof pageCSS !== 'undefined') { %>
        <link rel="stylesheet" href="<%= pageCSS %>">
    <% } %>
</head>
<body>
    <% if (typeof hideNav === 'undefined' || !hideNav) { %>
    <nav>
        <div class="nav-container">
            <a href="/map" class="nav-brand">🏰 Warlordlands</a>
            <ul class="nav-menu">
                <li><a href="/map">🗺️ Map</a></li>
                <li><a href="/armies">⚔️ Armies</a></li>
                <li><a href="/logout">🚪 Logout</a></li>
            </ul>
        </div>
    </nav>
    <% } %>

    <div class="container">
        <%- body %>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>

    <%- script %>
</body>
</html>
